import React, { FC } from "react";

// Define the props interface if the component takes any props
interface TestProps {
  // Define props here if needed
}

// Define the Test component with its props
const Test: FC<TestProps> = () => {
  return <div>Test</div>;
};

export default Test;
